# To run all pre-commit checks, use:
#
#     uvx prek run -a
#
# To install pre-commit hooks that run every time you commit:
#
#     uv tool install prek
#     prek install
#

ci:
  autoupdate_commit_msg: "⬆️🪝 update pre-commit hooks"
  autoupdate_schedule: quarterly
  autofix_commit_msg: "🎨 pre-commit fixes"
  skip: [ty]

repos:
  ## Standard hooks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-merge-conflict
        args: [--assume-in-merge]
        fail_fast: true
        priority: 0
      - id: end-of-file-fixer
        exclude: ^vendor/
        priority: 1
      - id: trailing-whitespace
        exclude: ^vendor/
        priority: 2

  ## Check JSON schemata
  - repo: https://github.com/python-jsonschema/check-jsonschema
    rev: 0.38.0
    hooks:
      - id: check-codecov
        priority: 0
      - id: check-github-workflows
        priority: 0
      - id: check-mergify
        priority: 0
      - id: check-readthedocs
        priority: 0

  ## Check for security issues in GitHub Actions workflows
  - repo: https://github.com/zizmorcore/zizmor-pre-commit
    rev: v1.29.0
    hooks:
      - id: zizmor
        priority: 0

  ## Check best practices for scientific Python code
  - repo: https://github.com/scientific-python/cookie
    rev: 2026.08.14
    hooks:
      - id: sp-repo-review
        additional_dependencies: ["repo-review[cli]"]
        priority: 0

  ## Check pyproject.toml file
  - repo: https://github.com/henryiii/validate-pyproject-schema-store
    rev: 2026.08.15
    hooks:
      - id: validate-pyproject
        priority: 0

  ## Ensure uv.lock is up to date
  - repo: https://github.com/astral-sh/uv-pre-commit
    rev: 0.12.5
    hooks:
      - id: uv-lock
        priority: 0

  ## Check for common capitalization mistakes
  - repo: local
    hooks:
      - id: disallow-caps
        name: Disallow improper capitalization
        language: pygrep
        entry: \b(Nanobind|Numpy|Cmake|CCache|Github|PyTest|Mqt|Tum)\b
        exclude: ^(\.pre-commit-config\.yaml|vendor/)
        priority: 0
      - id: disallow-const-mlir-handles
        name: Disallow const on named MLIR handles and views
        language: pygrep
        entry: >-
          (?x) \b(?: const\s+(?:(?:::)?mlir::)?(?:Value|TypedValue|BlockArgument|OpResult|ValueRange|OperandRange|ResultRange|Operation|Block|Region|ModuleOp)\b |
          (?:(?:::)?mlir::)?(?:Value|TypedValue|BlockArgument|OpResult|ValueRange|OperandRange|ResultRange|Operation|Block|Region|ModuleOp)\s+const\b |
          (?:(?:::)?mlir::)?TypedValue\s*<[^;\n]+>\s+const\b )
        files: ^(mlir|bindings/mlir)/.*\.(c|cc|cpp|cxx|h|hh|hpp|hxx)$
        priority: 0

  ## Check for typos
  - repo: https://github.com/adhtruong/mirrors-typos
    rev: v1.49.0
    hooks:
      - id: typos
        exclude: ^vendor/
        priority: 3

  ## Check license headers
  - repo: https://github.com/emzeat/mz-lictools
    rev: v2.9.0
    hooks:
      - id: license-tools
        exclude: ^vendor/
        priority: 4

  ## Format BibTeX files with bibtex-tidy
  - repo: https://github.com/FlamingTempura/bibtex-tidy
    rev: v1.14.0
    hooks:
      - id: bibtex-tidy
        args:
          [
            "--align=20",
            "--curly",
            "--months",
            "--blank-lines",
            "--sort",
            "--strip-enclosing-braces",
            "--sort-fields",
            "--trailing-commas",
            "--remove-empty-fields",
          ]
        priority: 5

  ## Format configuration files with prettier
  - repo: https://github.com/rbubley/mirrors-prettier
    rev: v3.9.6
    hooks:
      - id: prettier
        types_or: [yaml, html, css, scss, javascript, json, json5]
        priority: 5

  ## Format Markdown files with rumdl
  - repo: https://github.com/rvben/rumdl-pre-commit
    rev: v0.2.57
    hooks:
      - id: rumdl
        args: [--fix]
        priority: 5
      - id: rumdl-fmt
        priority: 6

  ## Format CMake files with cmake-format
  - repo: https://github.com/cheshirekow/cmake-format-precommit
    rev: v0.6.13
    hooks:
      - id: cmake-format
        additional_dependencies: [pyyaml]
        types: [file]
        files: (\.cmake|CMakeLists.txt)(.in)?$
        priority: 5

  ## Format C++ files with clang-format
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v22.1.8
    hooks:
      - id: clang-format
        types_or: [c++, c, cuda]
        exclude: ^vendor/
        priority: 5
      - id: clang-format
        name: clang-format (TableGen)
        types_or: [file]
        files: \.td$
        priority: 5

  ## Format and lint Python files with ruff
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.3
    hooks:
      - id: ruff-check
        require_serial: true
        priority: 6
      - id: ruff-format
        types_or: [python, pyi, jupyter, markdown]
        priority: 7

  ## Check Python types with ty
  - repo: https://github.com/astral-sh/ty-pre-commit
    rev: v0.0.73
    hooks:
      - id: ty
        args: [--only-dev]
        priority: 8
